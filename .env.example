# Neon Postgres 연결 문자열 (Vercel > Storage 에서 Neon을 붙이면 자동으로 주입됩니다)
# 로컬에서는 `vercel env pull .env` 로 받아오면 됩니다.
DATABASE_URL="postgres://user:password@host/db?sslmode=require"

# Vercel Blob 토큰. 배포 환경에는 자동 주입됩니다.
# 로컬에서는 비워두면 public/uploads 폴더에 저장합니다 (배포 환경에서는 동작하지 않음).
BLOB_READ_WRITE_TOKEN=""
